# -voice-order-agent
A voice agent that takes product orders over the phone — speech in, order in Postgres out. Open-source ASR, hybrid retrieval, and an eval harness that measures whether it actually works.
