# Optional. Everything has a working default -- copy this only if you want to
# move the database or the indexes somewhere else.

# SQLite file. Default: data/voice_order.db
# VOICE_ORDER_DB=data/voice_order.db

# On-disk retrieval indexes. Default: data/index/
# VOICE_ORDER_INDEX_DIR=data/index
